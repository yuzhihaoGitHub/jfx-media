<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 日志存放路径 -->
    <property name="log.path" value="logs"/>
    <!-- 日志存放时间 -->
    <property name="log.time" value="7"/>
    <!-- 日志输出格式 -->
    <property name="debug.pattern" value="%d{dd日HH:mm:ss.SSS} [%thread] %green(%-5level) %green(%logger{36}) - %magenta([%method,%line]) - %msg%n"/>
    <property name="info.pattern" value="%d{dd日HH:mm:ss.SSS} [%thread] %cyan(%-5level) %cyan(%logger{36}) - %magenta([%method,%line]) - %msg%n"/>
    <property name="warn.pattern" value="%d{dd日HH:mm:ss.SSS} [%thread] %yellow(%-5level) %yellow(%logger{36}) - %magenta([%method,%line]) - %msg%n"/>
    <property name="error.pattern" value="%d{dd日HH:mm:ss.SSS} [%thread] %red(%-5level) %red(%logger{36}) - %magenta([%method,%line]) - %msg%n"/>

    <!-- 控制台输出 -->
    <appender name="console_debug" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${debug.pattern}</pattern>
        </encoder>
    </appender>
    <appender name="console_info" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${info.pattern}</pattern>
        </encoder>
    </appender>
    <appender name="console_warn" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${warn.pattern}</pattern>
        </encoder>
    </appender>
    <appender name="console_error" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${error.pattern}</pattern>
        </encoder>
    </appender>

    <!-- 系统info日志保存 -->
    <appender name="file_info" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/sys-info.log</file>
        <!-- 循环政策：基于时间创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>200MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <!-- 日志文件名格式 -->
            <fileNamePattern>${log.path}/sys-info.%d.%i.log</fileNamePattern>
            <!-- 日志最大的历史 60天 -->
            <maxHistory>${log.time}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${info.pattern}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 过滤的级别 -->
            <level>INFO</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- 系统warn日志保存 -->
    <appender name="file_warn" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/sys-warn.log</file>
        <!-- 循环政策：基于时间创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>200MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <!-- 日志文件名格式 -->
            <fileNamePattern>${log.path}/sys-warn.%d.%i.log</fileNamePattern>
            <!-- 日志最大的历史 60天 -->
            <maxHistory>${log.time}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${warn.pattern}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 过滤的级别 -->
            <level>WARN</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- 系统error日志保存 -->
    <appender name="file_error" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/sys-error.log</file>
        <!-- 循环政策：基于时间创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志文件名格式 -->
            <fileNamePattern>${log.path}/sys-error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 日志最大的历史 60天 -->
            <maxHistory>${log.time}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${error.pattern}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 过滤的级别 -->
            <level>ERROR</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!--系统操作日志-->
    <root level="info">
        <appender-ref ref="console_debug"/>
        <appender-ref ref="console_info"/>
        <appender-ref ref="console_warn"/>
        <appender-ref ref="console_error"/>
        <appender-ref ref="file_info"/>
        <appender-ref ref="file_warn"/>
        <appender-ref ref="file_error"/>
    </root>


</configuration> 